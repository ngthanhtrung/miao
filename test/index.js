// 'use strict';

// var miao = require('../');

// miao({ base: __dirname }, function (err, stack) {
//   if (err) {
//     console.error(err && err.stack ? err.stack : err);
//     process.exit(1);
//   }

//   console.log(stack);
// });
